<div class="content">
	<h1><%= @title %></h1>
	<%= render 'comicNav' %>
	<%= image_tag(url_for(@strip.image), alt: @strip.transcript, class: 'comic') %>
	<!-- tags -->
	<%= render 'comicNav' %>
</div>
